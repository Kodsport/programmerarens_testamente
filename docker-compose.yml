services:
  website:
    container_name: pt-website
    build:
      context: website
      dockerfile: Dockerfile
    image: ksprt:latest
    ports:
      - 127.0.0.1:6969:6969

    volumes:
      - ./app/user:/app/user